using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class globalName : MonoBehaviour {
    //this script is just to keep the name of the curent chat window, so it will be easier to acces it on other scripts
    public string Name="Default";
}
